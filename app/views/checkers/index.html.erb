<h1><FONT color="#ff69b4">Personal Data Checker</h1></FONT>
<div class="container">
  <div class="row">
    <div class="col-lg-12 text-center">
      <div class="box">
        <p class="text">
          Enter your personal data<br>
          We calculate your BMI value and show a model who is nearest height from you<br>
          Corresponding height: 150-170
        </p>
      </div>
    </div>
  </div>
  <br>
  <div class="row col-lg-4 col-xs-offset-4">
    <div class="panel panel-danger">
      <div class="panel-heading text-center">
        Please enter your height and weight
      </div>
      <div class="panel-body col-xs-offset-1 text-center">
        <%= form_tag('/checkers', method: "POST") do %>
        <%= label_tag 'Height (cm): ' %>
        <%= text_field_tag :height %><br>

        <%= label_tag 'Weight (kg):' %>
        <%= text_field_tag :weight %><br><br>

        <%= submit_tag 'Calculate', class: 'btn btn-large btn-danger' %>
        <% end %>

        <% if false %>
        <% PersonalDatabase.all.each do |record| %>
        <%= record.name %><br>
        <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

