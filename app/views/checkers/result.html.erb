<h1><FONT color="#ff69b4">Personal Data Checker</h1></FONT>
<div class="container">
  <div class="row">
    <div class="col-lg-12 text-center">
      <div class="box">
        <p class="text">
          Enter your personal data<br>
          We calculate your BMI value and show a model who is nearest height from you
        </p>
      </div>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col-lg-4 text-center">
      <p>your BMI is...</p>
      <div class="round">
        <%= @bmi.round(2) %>
      </div>
      <br>
      <p><%= @judge %></p>
    </div>
    <p>The nearest height model is...</p>
    <div class="col-lg-1">
      <% @model.each do |model| %>
      <%= image_tag model.image, alt: model.name, size: '160x200' %>
    </div>
      <div class="col-lg-6">
        <p class="text-center">
          <FONT size="6px"><%= model.name %></Font><br>
          (<%= model.height %>cm, <%= model.weight %>kg)<br>
        </p>
        <div class="row text-center">
          <br>
          <p> the difference is...</p>
          <div class="round">
            <%= (model.weight-@weight).round(2)%>kg
          </div>
      </div>
   </div>
      <% end %>
    </div>
  </div>
</div>
